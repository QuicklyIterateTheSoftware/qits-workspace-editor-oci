# The bump train: follow the released workspace image.
#
# qits-workspace-daemon publishes `qits/workspace:<version>` — the toolchain with the workspace daemon
# compiled in — and nothing floating, by its own ONE TAG PER PIPELINE rule. So `Dockerfile` here pins
# that image by version on one line, and this file is what moves that line.
#
# IT IS THE SAME TRAIN ONE LEVEL DOWN. qits-workspace-daemon follows qits-workspace-oci's
# `qits/workspace-base`; this repository follows qits-workspace-daemon's `qits/workspace`. A toolchain
# release therefore reaches the editor image by two hops, each of which is an ordinary release of the
# repository in between — never a rebuild chained through somebody else's pipeline.
#
# THE FILE IS NAMED AFTER THE UPSTREAM REPOSITORY, not after the file it was copied from. The
# `ci-event-upstream-oci-workspace.yml` beside qits-workspace-daemon follows qits-workspace-oci; this
# one follows qits-workspace-daemon, and a name saying otherwise would send the next reader to the
# wrong repository. qits-ci globs `ci-event-*.yml`, so the segment after the prefix is documentation
# and nothing else — which is exactly why it has to be true. (qits-projects-service's
# `ci-event-upstream-workspace-daemon.yml` follows this same upstream under this same name.)
#
# WHAT IT DOES NOT DO IS RELEASE. It writes one line and pushes a branch. The `maintenance/` leg in
# ci-post-receive.yml sees that push and calls the release endpoint; ci-event-release.yml then
# rebuilds the editor image on the new workspace. Three files, one hand-off each, and no step doing
# two jobs.
#
# WHY THE BRANCH IS FORCE-PUSHED AND ALWAYS THE SAME NAME. Two workspace releases in a row must not
# leave two open branches racing to release the same repository; the second overwrites the first, and
# the pin it carries is the newer one either way. The branch is named after the upstream repository so
# a different train never collides with this one.
#
# --- the event -------------------------------------------------------------------------------------
# `SoftwareRelease`, NOT `SCMRelease`, AND THE DIFFERENCE IS A RACE. SCMRelease fires when the
# workspace is TAGGED, which is before its ~3.4 GB image finishes pushing — so a pin could land, get
# released, and reach `docker build` while the base it names did not exist yet. SoftwareRelease is
# qits-ci announcing that the artifact is IN the registry, which is the fact this file actually needs.
#
# THE TWO SELECTORS ARE READ OFF THE UPSTREAM'S OWN DECLARATION, not guessed: qits-workspace-daemon's
# `.config/qits/ci-event-release.yml` declares `artifacts: [{ type: docker, name: qits/workspace }]`,
# and qits-ci turns the declared `type` into `packageType` and the declared `name` into `packageName`
# verbatim — the keyword IS the wire value. A docker image name never changes, so the pair carries the
# match on its own and survives every rename of the repository behind it.
#
# THERE IS NO `repository:` CONDITION HERE, AND ITS ABSENCE IS DELIBERATE — INHERITED AS A SCAR.
# `repository` on a SoftwareRelease is qits-ci's own STORAGE id for the run, which has been an opaque
# UUID since the 2026-08-22 identity cutover; a name written there matches nothing, silently, because
# CI logs matches and never non-matches. That is what killed the identical train in
# qits-workspace-daemon (see the comment at its `ci-event-upstream-oci-workspace.yml:30-36`), which
# sat pinned at a stale toolchain for a week while the upstream released twice. Do not add one here
# "for narrowness": the pair below is already narrow, since a repository publishing several images
# emits one of these per artifact and `packageName` is the one that says which. Contrast
# ci-event-release.yml beside this file, which selects an `SCMPublishTag` by `repoName` — every event
# names the repository with the field its own publisher holds, and they are not interchangeable.
event: SoftwareRelease
when:
  - packageType: { exact: docker }
    packageName: { exact: "qits/workspace" }
steps:
    # ci-base: git to commit and push, jq to read the payload, curl for the registry probe. No docker
    # socket — this step builds nothing, and it deliberately does not pull the image it is bumping
    # towards.
  - image: qits/build-images/ci-base:latest
    timeout-seconds: 600
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this a failed `git commit` would
      # be swallowed and the push would ship whatever was already on the branch. -u stops the step
      # on an unset injected variable rather than pushing to an empty url.
      set -eu

      # The payload reaches the container verbatim as one string in $QITS_EVENT_PAYLOAD; there is no
      # per-field projection. `jq -er` fails the step on an absent field rather than writing the
      # word "null" into the pin.
      version=$(printf '%s' "$QITS_EVENT_PAYLOAD" | jq -er .version)
      # Refused before it is written, because this value becomes a docker TAG in Dockerfile and part
      # of a URL just below. A CalVer is digits and dots; anything carrying `:` or `/` would make a
      # later release pull a different image, and the release pipeline's own guard would then be
      # rejecting a value this train had already committed. It also keeps `#` out of the `sed`
      # expression below, where `#` is the delimiter — a version carrying one closes the expression,
      # and GNU sed's `e` command then runs a shell.
      case "$version" in
        ''|*[!0-9.]*) echo "SoftwareRelease carries no valid CalVer: $version" >&2; exit 1 ;;
      esac

      # THE TAG HAS TO EXIST BEFORE THE PIN NAMES IT. A dangling pin is not a red build here — it is
      # a red build two hand-offs later, in a release pipeline, blaming a Dockerfile that was correct
      # when it was written.
      #
      # $QITS_REGISTRY IS THE WRONG ADDRESS FOR THIS AND THAT IS THE TRAP WORTH SPELLING. It is the
      # HOST DAEMON's view of the registry (`localhost:8081` on the local stack), usable only on the
      # far side of a mounted docker socket; inside a step container `localhost` is the step
      # container. The registry's in-network address is not injected on its own, but qits-artifacts
      # serves the OCI Distribution API at its HOST ROOT `/v2` — it has no choice, since a client
      # resolves a reference against `<host>/v2/` and accepts no path prefix — and
      # $QITS_MAVEN_REGISTRY_URL is the same deployment's origin plus a path. So the origin is taken
      # off that variable rather than written here: no deployment address is hard-coded, and moving
      # qits-artifacts moves both.
      artifacts_origin=$(printf '%s' "$QITS_MAVEN_REGISTRY_URL" | sed -E 's#^(https?://[^/]+).*#\1#')
      # HEAD, not GET: a manifest lookup is the existence check, and the Accept list is what makes a
      # registry answer with the manifest rather than 404 on a media type it will not negotiate. The
      # name is the packageName the selection above pins, so the probe asks about exactly the artifact
      # the event announced.
      curl -fsS -o /dev/null -I \
        -H "Accept: application/vnd.oci.image.index.v1+json, application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.v2+json, application/vnd.docker.distribution.manifest.list.v2+json" \
        "$artifacts_origin/v2/qits/workspace/manifests/$version"

      # THE PIN IS ONE ANCHORED LINE IN Dockerfile, and the prefix below is a contract with that
      # file's header. Written as a `sed` rather than a rewritten file because the pin lives with the
      # build that uses it: a committed `.version` file beside this one was a second place to read,
      # and the release pipeline had to cat it to compose a `--build-arg`.
      sed -i -E "s#^(ARG WORKSPACE_IMAGE=registry.dev.localhost:8080/qits/workspace:).*#\1$version#" \
        Dockerfile

      # A SED THAT MATCHES NOTHING IS SILENT, AND ITS SYMPTOM IS INDISTINGUISHABLE FROM SUCCESS —
      # both leave an empty diff, and the quiet exit below would then report a train that is up to
      # date while the pin sits at whatever it was. So the line is read back. Rename the image, move
      # the registry host or reflow that line, and this fails loudly in the commit that did it.
      grep -qxF "ARG WORKSPACE_IMAGE=registry.dev.localhost:8080/qits/workspace:$version" Dockerfile \
        || { echo "the base pin in Dockerfile did not take - has that line moved?" >&2; exit 1; }

      # ALREADY ON THAT VERSION IS A SUCCESS, NOT A NO-OP TO HIDE. A replayed event, or a workspace
      # release this repository already followed, must not push an empty commit and must not turn
      # the release train red.
      git add Dockerfile
      git diff --cached --quiet && exit 0

      git -c user.name="qits release train" -c user.email="release-train@qits.local" commit -q \
        -m "bump(qits-workspace-daemon@$version): follow the workspace image"
      git push --force "$QITS_CI_REPOSITORY_URL" HEAD:refs/heads/maintenance/qits-workspace-daemon
